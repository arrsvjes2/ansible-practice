---
- hosts: all
  tasks:
    - name: Update dnf cache and make sure Vim, Curl and Unzip are installed
      ansible.builtin.dnf:
        name: "{{ item }}"
        state: latest
      loop:
        - vim
        - curl
        - unzip
